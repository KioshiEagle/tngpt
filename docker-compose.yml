services:
  tngpt-app:
    image: ${IMAGE_TAG}
    container_name: tn-gpt-python
    ports:
      - "8501:8501"
    volumes:
      - hf_cache:/root/.cache/huggingface
    environment:
      - PYTHONUNBUFFERED=1
      - TRANSFORMERS_OFFLINE=1
    restart: always

volumes:
  hf_cache: