[pytest]
# Dossier où chercher les tests
testpaths = tests

# Options par défaut (v : verbeux, ra : affiche les raisons des skips/fails)
addopts = -v -ra

# Patterns pour identifier les fichiers de test
python_files = test_*.py