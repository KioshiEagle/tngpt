# secret
.env
app/back/service-account.json

# chiant sur le repo
.venv
app/back/__pycache__
app/__pycache__
core
.DS_Store
*.pyc
*cache
.python-version
*.db
.claude

# fichiers d'output
processed_files.json
processed_pdfs.json
*.pdf
app/back/temp/
Plaquette-Alpha**
docs/api/
site/

# état local Flask : contient le SQLite de repli, jamais la vraie base
instance/

# fichiers déposés dans le panel admin, en attente d'ingestion
uploads/

# NB : migrations/ doit être versionné — c'est lui qui construit le schéma
# en déploiement (`flask db upgrade`). Ne pas l'ignorer.